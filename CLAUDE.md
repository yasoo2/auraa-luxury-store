# Auraa Luxury — قواعد العمل في هذا المستودع

## القاعدة الأولى: لا شيء وهمي

هذا متجر حقيقي يبيع لعملاء حقيقيين. لا يُسمح بأي بيانات مُختلَقة أو سلوك
تمثيلي في أي مكان من المشروع:

- **ممنوع** أن تعرض صفحة أرقاماً مكتوبة في الكود عند فشل النداء. القائمة
  الفارغة الصادقة أفضل من قائمة مليئة بالكذب، ورسالة الخطأ أفضل من الاثنتين.
- **ممنوع** أن يُظهر زرٌّ نجاحاً لم يحدث. إن فشل الحفظ فلا تُحدَّث الشاشة ولا
  تُغلق النافذة — يُقال للمستخدم ما الذي فشل.
- **ممنوع** أن تبقى صفحة في القائمة وهي تنادي مساراً غير مكتوب في الخادم.
  إمّا يُكتب المسار أو تُزال الصفحة حتى يُكتب.
- **ممنوع** إبقاء كود ميت أو ملفات لا يستوردها أحد.

سبب القاعدة: البيانات الوهمية لا تخفي نفسها فحسب، بل تخفي أعطالاً حقيقية
تحتها. صفحة المستخدمين في لوحة التحكم لم تعمل ببيانات حقيقية ولا مرّة واحدة —
كانت تنهار على أول مستخدم — ولم يظهر ذلك قطّ لأن البيانات الوهمية كانت تحمل
الحقول التي تبحث عنها الصفحة.

## القاعدة الثانية: اكتشفتَ فأصلِح

عند اكتشاف عطل أثناء أي مهمة: أصلحه في حينه، لا تكتفِ بذكره.
وعند اكتشاف ما يحتاج تطويراً: طوّره فوراً دون سؤال.

## التحقّق قبل الادّعاء

لا يُقال «يعمل» إلا بعد إثبات. المعايير المتّبعة في هذا المستودع:

- **الواجهة:** فحص بمتصفّح حقيقي (Playwright)، والسؤال ليس «هل العنصر داخل
  الشاشة» بل `document.elementFromPoint` عند مركزه — أي هل هذا فعلاً ما
  سيضغطه المستخدم.
- **الاختبار الذي يمرّ دائماً لا قيمة له.** بعد كتابة اختبار لعطل، أزِل
  الإصلاح مؤقتاً وتأكّد أن الاختبار يفشل، ثم أعده. حدث مرّتين في هذا المشروع
  أن اختباراً كُتب لعطل وكان يمرّ قبل الإصلاح وبعده.
- **الخلفية:** `./scripts/verify.sh` يجب أن يكون أخضر قبل أي دفع.

## بنية المشروع

- `backend/` — FastAPI + MongoDB. المسارات في `server.py` و`routes/`.
  `core/security.py` مصدر واحد لكل ما يخصّ الجلسات والصلاحيات.
- `frontend/` — React (CRA + CRACO) + Tailwind. لوحة التحكم في
  `src/pages/admin/`.
- `tests/test_integration.py` — الاختبارات الخلفية، تعمل بقاعدة في الذاكرة.
- `scripts/verify.sh` — **البوّابة الوحيدة الفعلية.** شغّله قبل كل دفع.

  لا شيء آخر يحرس هذا المستودع اليوم:
  `.github/workflows/ci.yml` مكتوب وصحيح، وGitHub Actions **يُشغّله فعلاً**
  عند كل دفع وطلب دمج — بلغ التشغيل رقم ٥٢ — لكن **لا جهاز يُخصَّص للمهامّ
  أبداً**. الدليل من واجهة GitHub البرمجية مباشرةً: المهمّة تُنشأ وتموت في
  ثانيتين، و`runner_id` يساوي صفراً و`runner_name` فارغ، ولا سجلّات
  تُنزَّل (404). لا اختبار يُنفَّذ ولا شيء يُفحص.

  أي أن الحجب على مستوى الحساب لا الملف، وليس عطلاً في YAML. والمستودع صار
  عامّاً — والمستودعات العامّة لها دقائق مجانية — فيبقى السبب المرجَّح حدّ
  الإنفاق أو وسيلة دفع فاشلة أو Actions معطَّل في الإعدادات. يُراجَع في
  Settings ← Billing and plans ← Spending limits، وSettings ← Actions ←
  General ← Actions permissions.

  ولا يُصلَح هذا من داخل المستودع: لا تُضِع وقتاً في تعديل ci.yml بحثاً عن
  عطل ليس فيه.

  وRender وCloudflare يبنيان من `main` مباشرة ولا يشغّلان أيّ فحص. أي أن
  ما لا يمسكه `verify.sh` يصل إلى العملاء.

## النشر

- **الخلفية:** Render، يبني تلقائياً من `main`.
- **الواجهة:** Cloudflare Pages، تبني تلقائياً من `main`.
- المتغيّرات الحسّاسة في لوحتَي Render وCloudflare، لا في المستودع.

## اللغة

المشروع عربي أولاً (RTL) مع إنجليزي. أي نص جديد يُكتب باللغتين، وأي تموضع
يستعمل الخصائص المنطقية (`inset-inline-start`) لا اليمين واليسار الثابتين.
